# In-product HTTP API reference

Show the caller contract inside the workbench: left-rail **API** drawer, and the Connections → API tab. Include function invoke, official `/v1/systemone` and `/v1/models`, list/describe, auth, error shape, and MCP tools. Copyable examples use the current origin. Default English copy.
