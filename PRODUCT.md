# Jev Workbench
单用户 macOS 本地浏览器工作台。用户将输入、独立判断问题、复核规则及输出映射发布为不可变函数版本，通过受限 HTTP/MCP/Pi 调用。管理在本地，推理在 TypeSafe 云端。成功路径：配置→真实试跑→发布→授权→调用。基础合同为 specs/v1/spec.md；用户确认的单页左右工作台与删除行为修订见 specs/single-page/spec.md。

界面采用用户指定的Coss UI中性风格，左侧函数列表，右侧当前内容；Noul/Choice/Score在新增入口直接可选。中文与English均支持。用户无TypeSafe Key时用显式独立演示，绝不将模拟成功宣称为真实推理。
